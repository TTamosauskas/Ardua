name: Deploy Ardua to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1

      - name: Open campaign process trails
        run: python .github/scripts/open_campaign_process_trails.py

      - name: Validate campaign map
        run: |
          node --check assets/js/campaign-map.js
          git diff --check

      - name: Commit open campaign trails
        run: |
          git config user.name "Ardua Automation"
          git config user.email "actions@users.noreply.github.com"
          git add assets/js/campaign-map.js assets/css/campaign-map.css
          if ! git diff --cached --quiet; then
            git commit -m "Open nucleosynthesis process trails [skip ci]"
            git push
          fi

      - name: Upload static site
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
