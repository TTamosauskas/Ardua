name: Apply explicit convection confirmation fix

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/apply-convection-confirm-flow.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0

      - name: Apply patch
        run: |
          python3 -m py_compile scripts/apply-convection-confirm-flow.py
          python3 scripts/apply-convection-confirm-flow.py

      - name: Validate convection interaction
        run: |
          node --check assets/js/ardua.js
          node scripts/validate-convection-regression.js
          node scripts/validate-coronal-jets.js
          node scripts/validate-stellar-plasma.js
          node scripts/validate-cumulative-recipes.js
          node scripts/validate-recipe-planner.js
          node scripts/validate-movement-chain-particles.js
          node scripts/validate-beta-parallel.js
          node scripts/validate-rotation-option.js
          node scripts/validate-recipe-audio-cadence.js
          node scripts/validate-menu-discoveries-ux.js
          node scripts/validate-campaign-phase-flow.js

      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/js/ardua.js scripts/validate-convection-regression.js scripts/validate-coronal-jets.js
          git diff --cached --quiet && exit 0
          git commit -m "fix: restore three-step convection interaction"
          git push origin HEAD:main
